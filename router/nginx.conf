events {
    use           epoll;
    worker_connections  128;
}

http {
    server {
        listen 80;
        
        location /displays/ {
            proxy_pass http://rendering:3000/;
        }

        location /gotrain/ {
            proxy_pass http://gotrain:8080/;
        }
    }
}